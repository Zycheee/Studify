import React from "react";

const EvolutionName = ({ currentPet, nextEvolution }) => {
  if (!currentPet) return null;

  return (
    <div style={{ textAlign: "center", marginTop: "20px" }}>
    </div>
  );
};

export default EvolutionName;
